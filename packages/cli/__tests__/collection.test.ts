import { describe, it, expect } from 'vitest'

describe('collection commands', () => {
  it('should format collection data', () => {
    // Basic test - detailed API tests are in api package
    expect(true).toBe(true)
  })
})
